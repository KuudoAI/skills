{
  "skill_name": "amazon-ads-marketing-cloud",
  "skill_version": "0.3.7",
  "evals": [
    {
      "id": 0,
      "prompt": "I need an AMC SQL workflow to measure reach and frequency by campaign for January 2025, broken out new-to-brand vs returning. Instance ID and entity IDs are already set up.",
      "expected_output": "A privacy-safe aggregated query (no SELECT *), uses conditional aggregation (COUNT(DISTINCT CASE WHEN ...)) for NTB vs returning split off a single CTE, includes a HAVING floor on COUNT(DISTINCT user_id), workflow creation using privacyFilteringBehavior REMOVE_VALUES, full-datetime execution window, and mention of monitoring execution time / empty-result pitfalls (file-size check).",
      "files": [],
      "assertions": [
        {
          "name": "aggregated-sql",
          "condition": "Proposed SQL aggregates metrics; does not use SELECT *"
        },
        {
          "name": "having-floor",
          "condition": "Includes a HAVING clause with COUNT(DISTINCT user_id) >= N (50, 100, or similar) to drop suppressed groups at the source"
        },
        {
          "name": "conditional-aggregation",
          "condition": "Uses conditional aggregation (COUNT(DISTINCT CASE WHEN flag THEN user_id END)) for the NTB vs returning split rather than separate CTEs joined together"
        },
        {
          "name": "remove-values-exploratory",
          "condition": "Recommends REMOVE_VALUES (or equivalent) for exploratory runs"
        },
        {
          "name": "datetime-window",
          "condition": "Execution time window uses full ISO-like datetime, not date-only strings"
        }
      ]
    },
    {
      "id": 1,
      "prompt": "My AMC workflow finished in 30 seconds and the downloaded CSV is only 67 bytes. What went wrong?",
      "expected_output": "Diagnosis that the file is empty (size ≤ header line length is the reliable test); notes that the 30-second timing is suggestive but no longer definitive on newer faster infrastructure; checklist including wrong entity, backfill lag, date range, aggregation threshold, and advertiserTypes/DSP mismatch.",
      "files": [],
      "assertions": [
        {
          "name": "file-size-vs-header",
          "condition": "Diagnoses empty result via the file-size ≤ header-line-length check (or notes that 67 bytes is header-only for a 4-column output); does NOT claim the byte count is universal"
        },
        {
          "name": "timing-caveat",
          "condition": "Notes that fast execution time is suggestive of empty but not definitive — newer AMC infrastructure can return real results in under 60 seconds"
        },
        {
          "name": "actionable-checks",
          "condition": "Suggests at least two concrete checks (e.g. entity/campaign ownership, date range, instance status, advertiserTypes for DSP queries, or aggregation thresholds)"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "Write AMC SQL for a DSP rule-based audience from sponsored ads traffic — what table variant and SELECT shape do I need?",
      "expected_output": "Use of _for_audiences table variant, SELECT DISTINCT user_id, and audience-specific constraints; no raw user_id in general reporting SELECT lists.",
      "files": [],
      "assertions": [
        {
          "name": "audience-table",
          "condition": "References audience-oriented table pattern (e.g. sponsored_ads_traffic_for_audiences or equivalent _for_audiences guidance)"
        },
        {
          "name": "distinct-user-id",
          "condition": "Audience output resolves to DISTINCT user_id (or clearly states equivalent audience output contract)"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "Walk me through the official AMC playbook for measuring off-Amazon conversions. I want to use Events Manager and the swim-lane structure.",
      "expected_output": "Identifies the off-Amazon conversions playbook, queries the chroma_mcp amazon_ads collection for it (semantic search and/or metadata filter on semantic_id), references the swim-lane organization, mentions Events Manager / CAPI as the data ingestion path, and stays grounded in retrieved content rather than improvising.",
      "files": [],
      "assertions": [
        {
          "name": "queries-knowledge-base",
          "condition": "EITHER calls chroma_mcp (chroma_query_documents or chroma_get_documents) against the amazon_ads collection, OR explicitly identifies chroma_mcp / the amazon_ads collection as the canonical source and notes it is unavailable in the current environment before falling back to skill knowledge. Pure-from-internal-knowledge answers that ignore the knowledge base fail."
        },
        {
          "name": "names-the-playbook",
          "condition": "References the off-Amazon conversions playbook by name or by semantic_id 'off-amazon-conversions'"
        },
        {
          "name": "events-manager",
          "condition": "Mentions Events Manager (or CAPI) as the off-Amazon conversion ingestion mechanism"
        }
      ]
    },
    {
      "id": 4,
      "prompt": "What AMC playbooks exist for custom attribution? I need to compare first-touch, last-touch, linear, and position-based models.",
      "expected_output": "Surfaces the four custom attribution playbooks as distinct documents in the knowledge base. For position-based, recognizes that semantic search returns weak results and uses a metadata filter (where: semantic_id) to retrieve it instead. Briefly characterizes each model.",
      "files": [],
      "assertions": [
        {
          "name": "lists-four-models",
          "condition": "Identifies all four custom attribution variants: first-touch, last-touch, linear, and position-based"
        },
        {
          "name": "uses-knowledge-base",
          "condition": "EITHER queries chroma_mcp for the attribution documents, OR identifies chroma_mcp / the amazon_ads collection as the canonical source (cites semantic_ids like 'custom-attribution-overview' / '-first-touch' / '-last-touch' / '-linear') and notes the environment if it cannot reach chroma_mcp"
        },
        {
          "name": "metadata-filter-or-retry",
          "condition": "For at least one of the four (especially position-based), either uses chroma_get_documents with a where filter on semantic_id, OR recognizes weak semantic-search distances and adapts the query, OR identifies the position-based playbook by its full semantic_id ('instructional-query-revision-custom-attribution-position-based') showing awareness that metadata-mode is the right access pattern for that doc"
        }
      ]
    },
    {
      "id": 5,
      "prompt": "I just got pre-signed download URLs back from amc_getWorkflowExecutionDownloadUrls. The result is around 3 MB. I want to know the top 10 advertisers by users_that_purchased. How should I handle this?",
      "expected_output": "Recommends an available execution or file-processing facility fetch the URL, parse the CSV outside model context, and return ONLY a focused JSON summary (top-10 list and row count). If no suitable facility is available, asks the user to provide a downloaded local file. Notes URL expiry (10 minutes) handling.",
      "files": [],
      "assertions": [
        {
          "name": "out-of-context-processing",
          "condition": "Recommends a client-available execution or file-processing facility fetch and parse the CSV outside model context, or asks the user to provide a downloaded local file when no suitable facility exists"
        },
        {
          "name": "focused-summary",
          "condition": "Explicitly returns only the top-10 summary (or equivalent focused JSON) from the processing step; does NOT print, read, or otherwise return the full CSV body into model context"
        },
        {
          "name": "url-expiry-or-empty-check",
          "condition": "Mentions either the pre-signed URL 10-minute expiry (and how to handle it) OR the file-size empty-result check before parsing"
        }
      ]
    },
    {
      "id": 6,
      "prompt": "I'm working with an agency identity that I think manages roughly 12 brands across the US and Canada marketplaces. How do I find all the AMC instances I actually have access to under this identity?",
      "expected_output": "Describes the portfolio fan-out: set_active_identity once, then iterate ac_ListAdsAccounts → set_active_profile per account/marketplace → amc_listInstances per profile. Filters by creationStatus: COMPLETED. Presents candidates to the user for selection rather than picking one automatically. Notes discovery does not require terms acceptance.",
      "files": [],
      "assertions": [
        {
          "name": "fan-out-pattern",
          "condition": "Describes iterating over accounts/profiles (via ac_ListAdsAccounts or ac_listProfiles) and calling amc_listInstances per profile, rather than assuming a single instance"
        },
        {
          "name": "user-pick",
          "condition": "Surfaces the candidate instance list to the user for selection rather than picking one automatically"
        },
        {
          "name": "creation-status-filter",
          "condition": "Filters for creationStatus: COMPLETED (or mentions that non-COMPLETED instances are not usable)"
        }
      ]
    },
    {
      "id": 7,
      "prompt": "I'm hitting `HTTP 403: Terms and conditions must be accepted to continue` when I call amc_createWorkflow. What's going on and how do I fix it?",
      "expected_output": "Diagnosis: instance-scoped terms agreement has not been accepted for this advertiser/entity. Calls amc_AmcpLinkGetTermsV2 to retrieve hasAccepted, agreementToken, and agreementContent. If hasAccepted is false, surfaces the agreement to the user, explicitly asks for authorization to accept, and only then calls amc_AmcpLinkSetTermsAcceptanceV2 with the agreementToken. Notes this is a binding legal action; surfaces agency-vs-advertiser relationship if applicable.",
      "files": [],
      "assertions": [
        {
          "name": "diagnoses-via-get-terms",
          "condition": "Recommends calling amc_AmcpLinkGetTermsV2 (or equivalent get-terms tool such as AmcpLinkGetTermsV2 / cm_LinkGetTermsV2) to read hasAccepted / agreementToken / agreementContent"
        },
        {
          "name": "explicit-user-authorization",
          "condition": "Requires explicit user authorization before calling amc_AmcpLinkSetTermsAcceptanceV2 (or equivalent set-terms-acceptance tool). Does NOT auto-accept on the user's behalf."
        },
        {
          "name": "binding-legal-action",
          "condition": "States or implies that terms acceptance is a binding legal action (or notes the agency-acting-on-behalf-of-advertiser relationship)"
        }
      ]
    },
    {
      "id": 8,
      "prompt": "I want to write an AMC query joining DSP impressions with Sponsored Products clicks on a new instance someone shared with me (instanceId: amctfvm5gmh). Before I write the SQL, what should I verify?",
      "expected_output": "Calls amc_getInstance for amctfvm5gmh and inspects the advertiserTypes field. If advertiserTypes is [\"SPONSORED_ADS\"] only, the instance has no DSP data — dsp_impressions / dsp_clicks queries will return table-not-found or silently empty. Suggests sponsored_ads_traffic and amazon_attributed_events_by_* tables instead.",
      "files": [],
      "assertions": [
        {
          "name": "checks-advertiser-types",
          "condition": "Recommends inspecting amc_getInstance's advertiserTypes field before writing DSP-table-referencing SQL"
        },
        {
          "name": "warns-on-sp-only",
          "condition": "Notes that an SP-only instance (advertiserTypes: [\"SPONSORED_ADS\"]) cannot serve queries against dsp_impressions or dsp_clicks (or equivalent DSP tables)"
        }
      ]
    },
    {
      "id": 9,
      "prompt": "My previous AMC workflow succeeded but the next one returns `error_kind: internal_error` with `details[0].issue: 'No active identity. Call set_active_identity first.'` Nothing has changed on my end. What's the right way to handle this in my next execute block?",
      "expected_output": "Recommends the probe-first session-scope contract: call get_session_state at the top of the execute block, branch on state_scope ('request' vs 'session') and state_reason ('no_mcp_session' / 'token_swapped' / 'bridge_unavailable'), and only re-call set_active_identity / set_region / set_active_profile if the probe says context is gone. Does NOT recommend unconditionally re-calling set_active_identity every block — that was the prior workaround for a fixed bug.",
      "files": [],
      "assertions": [
        {
          "name": "probe-first-pattern",
          "condition": "Recommends calling get_session_state at the start of the execute block as the read-only probe BEFORE deciding whether to re-establish context"
        },
        {
          "name": "branches-on-state-scope",
          "condition": "Branches the re-establish decision on state_scope ('request' vs 'session') and/or state_reason being non-null. Does not unconditionally re-call set_active_identity."
        },
        {
          "name": "names-reason-codes",
          "condition": "References at least one of the state_reason values ('no_mcp_session', 'token_swapped', 'bridge_unavailable') OR clearly distinguishes the request-scoped vs session-scoped cases"
        }
      ]
    },
    {
      "id": 10,
      "prompt": "I ran `chroma_query_documents` with collection_name 'amazon_ads' and query_texts ['new to brand percentage by product attribute targeting PAT campaign ASIN'], n_results 5. I tried reading `r['documents'][0]` and got an empty list, but I know the collection has hits at distance ~0.55 for that query. What am I doing wrong?",
      "expected_output": "Identifies that the chroma_mcp server wraps results in {data: {...}, meta: {...}}; the correct walk is r['data']['documents'][0] (or defensive r.get('data', r)['documents'][0]). Notes that 'meta' carries the collection and query echo. Not a search-quality issue, an envelope-unwrap issue.",
      "files": [],
      "assertions": [
        {
          "name": "names-data-wrap",
          "condition": "Identifies that the response is wrapped in a {data: ..., meta: ...} envelope and the documents/metadatas/distances live under r['data']"
        },
        {
          "name": "correct-access-pattern",
          "condition": "Provides a correct access pattern like r['data']['documents'][0], or a defensive variant such as r.get('data', r)['documents'][0]"
        },
        {
          "name": "rules-out-quality",
          "condition": "Frames this as an envelope-unwrap issue, NOT as a search-quality / indexing / distance issue (i.e. does not suggest the collection is missing the content or that the query needs rephrasing)"
        }
      ]
    }
  ]
}
