{
  "skill_name": "amazon-ads-reporting",
  "skill_version": "0.3.1",
  "evals": [
    {
      "id": 0,
      "prompt": "I need the Sponsored Products Campaign report for advertiser account amzn1.ads-account.g.ABC123XYZ for the period 2026-04-01 through 2026-04-27. Return a CreateReport JSON I can submit to the Amazon Ads v1 reporting endpoint.",
      "expected_output": "Returns the SP Campaign CreateReport JSON with the user's advertiser account ID and date range filled in, the singular query.filter.on predicate scoping to SPONSORED_PRODUCTS via comparisonOperator EQUALS, periods at the report level with a datePeriod, and a short caveats summary.",
      "files": [],
      "assertions": [
        {
          "name": "returns-create-report-json",
          "condition": "Output contains a JSON object with `accessRequestedAccounts` and `reports` arrays, where each report has `format`, `periods`, and `query` blocks — the v1 CreateReport shape"
        },
        {
          "name": "advertiser-id-filled",
          "condition": "advertiserAccountId is amzn1.ads-account.g.ABC123XYZ (the account the user provided), not the REPLACE_ME placeholder"
        },
        {
          "name": "dates-in-periods-not-timerange",
          "condition": "Date range lives at reports[0].periods[0].datePeriod with startDate=2026-04-01 and endDate=2026-04-27. The output does NOT include a query.timeRange field (that field doesn't exist on the v1 endpoint)."
        },
        {
          "name": "filter-singular-on-shape",
          "condition": "Output uses singular query.filter (not plural query.filters[]) and the filter wraps a comparison predicate at filter.on with the keys field, comparisonOperator, not, and values"
        },
        {
          "name": "comparison-operator-equals",
          "condition": "comparisonOperator value is EQUALS (not EQUAL_TO, not equals, not =) — and the output does NOT use an `operator` field name"
        },
        {
          "name": "ad-product-filter-sp",
          "condition": "filter.on.field is adProduct.value and filter.on.values is exactly [SPONSORED_PRODUCTS]"
        },
        {
          "name": "core-sp-campaign-fields",
          "condition": "fields[] includes date.value, campaign.id, campaign.name, budgetCurrency.value, metric.impressions, metric.clicks, metric.totalCost, and metric.sales (the validated SP campaign field set; budgetCurrency.value is required by the v1 catalog whenever cost or sales metrics are queried)"
        },
        {
          "name": "names-v3-report",
          "condition": "Response identifies the report as the Sponsored Products Campaign report (or v3 SP Campaign equivalent)"
        }
      ]
    },
    {
      "id": 1,
      "prompt": "Build me the Sponsored Brands Keyword report on v1 — I want keyword-grain performance plus Top-of-search Impression Share. Account is amzn1.ads-account.g.SBKEYWORDACCT, dates 2026-03-01 to 2026-03-31.",
      "expected_output": "Returns BOTH sub-reports: the main keyword-grain report and a separate campaign-grain report for metric.topOfSearchImpressionShare, each with its own periods, with the SPONSORED_BRANDS filter (singular filter.on shape, EQUALS operator) on each, and explains the client-side LEFT JOIN on (date.value, campaign.id).",
      "files": [],
      "assertions": [
        {
          "name": "two-sub-reports",
          "condition": "Output presents two separate, directly submittable CreateReport JSON bodies, one for the main keyword report and one for top-of-search impression share. It does not present the documentation wrapper itself as an API request."
        },
        {
          "name": "main-has-keyword-grain",
          "condition": "The main sub-report's fields[] includes target.value, target.matchType, and adGroup.id (keyword/target grain), and includes metric.impressions, metric.clicks, metric.sales"
        },
        {
          "name": "tos-has-campaign-grain",
          "condition": "The TOS sub-report's fields[] includes metric.topOfSearchImpressionShare and is at the campaign grain (no adGroup.* or target.* fields)"
        },
        {
          "name": "ad-product-filter-sb",
          "condition": "Both sub-reports use a singular query.filter.on predicate with comparisonOperator EQUALS and values [SPONSORED_BRANDS] — not SPONSORED_PRODUCTS, not SPONSORED_DISPLAY, not missing"
        },
        {
          "name": "periods-at-report-level",
          "condition": "Each sub-report has its own reports[0].periods[0].datePeriod with startDate=2026-03-01 and endDate=2026-03-31. Neither sub-report uses query.timeRange."
        },
        {
          "name": "explains-join-key",
          "condition": "Output tells the user to LEFT JOIN the TOS report onto the main report on (date.value, campaign.id) client-side"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "Give me the v1 equivalent of the Sponsored Brands Category Benchmark report — I want peer percentile comparisons for impressions, CTR, ACOS, and ROAS.",
      "expected_output": "Explicitly states the report is NOT REPRODUCIBLE on the v1 endpoint, names the missing peer/percentile metrics and missing brand/category dimensions, and points to the reference doc and recovery paths. Does not fabricate a JSON template.",
      "files": [],
      "assertions": [
        {
          "name": "states-not-reproducible",
          "condition": "Output explicitly says the v3 SB Category Benchmark report is not reproducible / has no v1 equivalent — does not pretend it can be built"
        },
        {
          "name": "names-missing-fields",
          "condition": "Output identifies the missing capabilities — peer/percentile metrics (e.g. metric.peerImpressions, metric.peerCtr, metric.peerAcos, metric.peerRoas) and/or advertiser-level brand/category dimensions"
        },
        {
          "name": "no-fabricated-json",
          "condition": "Output does NOT contain a CreateReport JSON body presented as if it reproduces the Category Benchmark report. (A snippet showing the advertiser's own impressions/CTR is acceptable only if it is clearly labelled as a partial substitute.)"
        },
        {
          "name": "mentions-recovery-path",
          "condition": "Output points the user toward an alternate API or approach (e.g. Brand Metrics, Brand Health, dedicated benchmarking endpoint) or the reference doc that explains the gap"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "Reproduce the v3 Sponsored Display Targeting report on v1. Account amzn1.ads-account.g.SDTARG777, last 14 days ending 2026-04-27.",
      "expected_output": "Returns the SD Targeting CreateReport JSON with a singular query.filter.on predicate scoping to SPONSORED_DISPLAY via EQUALS, target.* dimensions, and reports[0].periods[0].datePeriod covering 2026-04-14 to 2026-04-27 (last 14 days inclusive of the end date).",
      "files": [],
      "assertions": [
        {
          "name": "ad-product-filter-sd",
          "condition": "query.filter.on has field=adProduct.value, comparisonOperator=EQUALS, and values=[SPONSORED_DISPLAY] (not SPONSORED_PRODUCTS, not SPONSORED_BRANDS)"
        },
        {
          "name": "filter-shape-correct",
          "condition": "Output uses singular query.filter (not query.filters[]) and includes the not:false boolean on the comparison predicate"
        },
        {
          "name": "targeting-grain",
          "condition": "fields[] includes target.value (and other target/campaign-grain dimensions) — the SD targeting grain"
        },
        {
          "name": "advertiser-id-filled",
          "condition": "advertiserAccountId is amzn1.ads-account.g.SDTARG777, not the REPLACE_ME placeholder"
        },
        {
          "name": "date-range-in-periods",
          "condition": "reports[0].periods[0].datePeriod has startDate=2026-04-14 and endDate=2026-04-27 (14 days ending 2026-04-27), or the response asks the user to confirm this interpretation rather than silently picking a different range. The date range MUST be at the report level (periods.datePeriod), not in any query.timeRange field."
        }
      ]
    },
    {
      "id": 4,
      "prompt": "Can you generate the keyword report for me? Account is amzn1.ads-account.g.KW999, dates 2026-04-01 to 2026-04-27.",
      "expected_output": "Recognizes the request is ambiguous — the catalog has multiple keyword-related reports (SB Keyword, SB Keyword Placement) — and asks which one before producing JSON. Does not silently pick one.",
      "files": [],
      "assertions": [
        {
          "name": "asks-which-keyword-report",
          "condition": "Output asks the user to clarify which keyword report they want (SB Keyword vs SB Keyword Placement, or which ad product) — does not silently pick one and return its JSON"
        },
        {
          "name": "lists-options",
          "condition": "Output names the candidate reports — at minimum mentions both SB Keyword and SB Keyword Placement (or otherwise enumerates the keyword-related reports the catalog supports)"
        },
        {
          "name": "no-guess",
          "condition": "Output does NOT return a finalised CreateReport JSON before the ambiguity is resolved"
        }
      ]
    },
    {
      "id": 5,
      "prompt": "Build me the Sponsored Brands keyword report. Account amzn1.ads-account.g.SBAMBIG, dates 2026-04-01 to 2026-04-07.",
      "expected_output": "Recognizes that 'SB keyword' is ambiguous within Sponsored Brands — the catalog has both SB Keyword and SB Keyword Placement — and asks which one before producing JSON. Does not silently pick one even though the ad product is named.",
      "files": [],
      "assertions": [
        {
          "name": "ad-product-named-but-still-asks",
          "condition": "Even though Sponsored Brands is explicitly named, the response asks the user to disambiguate between SB Keyword and SB Keyword Placement (or otherwise lists the candidate SB-keyword-family reports). It does NOT silently default to one."
        },
        {
          "name": "names-both-candidates",
          "condition": "Response specifically names both SB Keyword (the keyword-grain performance report) and SB Keyword Placement as the two candidates (or otherwise enumerates the SB keyword-related catalog rows)"
        },
        {
          "name": "no-json-before-clarification",
          "condition": "Output does NOT contain a finalised CreateReport JSON body before the ambiguity is resolved"
        }
      ]
    },
    {
      "id": 6,
      "prompt": "I need the Sponsored Brands Brand Metrics report on v1. Account amzn1.ads-account.g.NOTACATALOG, dates 2026-04-01 to 2026-04-27.",
      "expected_output": "States the report is not in the catalog (or not supported by this skill) and stops. Does not fabricate a plausible-looking JSON body for a report this skill does not support.",
      "files": [],
      "assertions": [
        {
          "name": "refuses-uncatalogued",
          "condition": "Output explicitly says the SB Brand Metrics report is not in the skill's catalog / not supported by this skill / not one of the 17 reproducible reports — rather than treating it as a known report"
        },
        {
          "name": "no-fabricated-json",
          "condition": "Output does NOT contain a CreateReport JSON body presented as if it reproduces the SB Brand Metrics report. The skill's contract is no guessing, including not synthesising a body for a report it does not recognise."
        },
        {
          "name": "lists-supported-or-points-to-catalog",
          "condition": "Output points the user toward the catalog (references/report-catalog.md) or lists at least a sample of supported v3 reports the skill DOES reproduce, so the user can pick a real one"
        }
      ]
    },
    {
      "id": 7,
      "prompt": "Show daily Sponsored Products spend and sales by campaign for last week. I do not need one of the legacy named reports.",
      "expected_output": "Uses the ad-hoc path: confirms account scope and dates, selects a daily time dimension plus campaign identifiers and monetary metrics, includes the required currency dimension and SP filter, and validates the complete field list before presenting or submitting a request.",
      "files": [],
      "assertions": [
        {
          "name": "routes-ad-hoc",
          "condition": "Treats the request as a custom metric-and-grain report rather than silently choosing a legacy catalog template"
        },
        {
          "name": "states-grain-and-fields",
          "condition": "Uses daily and campaign grain, includes spend/sales fields and the required currency dimension, and scopes the request to Sponsored Products"
        },
        {
          "name": "validates-custom-fields",
          "condition": "Validates the complete custom field list against the current reporting catalog before live submission"
        }
      ]
    },
    {
      "id": 8,
      "prompt": "Submit the SP Campaign report for whichever account you find first and then keep polling until it is done.",
      "expected_output": "Does not choose the first account. It resolves and confirms advertiser-account scope, dates and request details before creating external report jobs, then uses bounded polling or returns a resumable report ID instead of polling indefinitely.",
      "files": [],
      "assertions": [
        {
          "name": "does-not-pick-first-account",
          "condition": "Lists or confirms matching advertiser accounts rather than choosing the first account silently"
        },
        {
          "name": "binds-live-request",
          "condition": "Confirms the concrete account, period, fields, filters, and number of report requests before submission"
        },
        {
          "name": "bounded-polling",
          "condition": "Uses bounded backoff or a host task/wait mechanism and preserves the report ID when the report is not terminal"
        }
      ]
    }
  ]
}
